name: Export Charmed Life Russian EPUB

on:
  push:
    branches:
      - assistant/charmed-life-ru-epub-export
    paths:
      - .github/workflows/export-charmed-life-ru.yml
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/export-charmed-life-ru.yml

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: assistant/charmed-life-ru-epub-export

      - name: Prepare Russian EPUB
        shell: bash
        run: |
          source_file='source/EPUB/RU/Djons_Diana_Uinn_[Miryi_Krestomansi#1]_Zakoldovannaya_jizn.epub'
          test -f "$source_file"
          cp "$source_file" /tmp/charmed-life-ru.epub
          ls -lh /tmp/charmed-life-ru.epub

      - name: Upload Russian EPUB
        uses: actions/upload-artifact@v4
        with:
          name: charmed-life-ru-source
          path: /tmp/charmed-life-ru.epub
          if-no-files-found: error
          retention-days: 1
